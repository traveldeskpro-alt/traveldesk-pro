// Demo dashboard — re-uses the production page component.
// Data hooks inside it read from localStorage via DataModeProvider.
// useAuth() returns demo values via DemoProvider.
export { default } from "@/app/dashboard/page";
